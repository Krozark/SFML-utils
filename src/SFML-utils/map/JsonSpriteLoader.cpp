#include <SFML-utils/map/JsonSpriteLoader.hpp>

#include <utils/json/Driver.hpp>

namespace sfutils
{
    namespace map
    {
        std::unordered_map<std::string,sf::IntRect> parse(const std::string& filename)
        {
            std::unordered_map<std::string,sf::IntRect> res;
            return res;
        }
    }
}
